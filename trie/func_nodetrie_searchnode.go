package trie

func (n *nodeTrie) searchNode(part string) *nodeTrie {
	/*
		个人间接：如果想要找到某个结点，可以先看一下每个结点的属性，
		比如说结点A，它包含路径、数据、子结点等属性。
		路径属性具有唯一性，所以可以通过路径属性来找到某个结点。
		数据属性不具有唯一性，子节点属性可能不具有唯一性，且使用子节点查找难度很大。
	*/
	return nil
}
